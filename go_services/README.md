# Go Services

`go_services/` is the optional remote/backend layer for ARX Platform v3.0.

Its role is limited to:
- telemetry gateway
- WebSocket fan-out
- REST API exposure
- remote diagnostics
- cloud synchronization

The core engine remains fully functional without these services.
